# 人机协作报告

## 一、AI 交互日志

### 模块1：m1.py — 数据清洗策略（data_cleaning_strategy）

| 项目 | 内容 |
|------|------|
| **Prompt** | "写一个数据清洗函数，对DataFrame自动处理缺失值（按缺失率分级：>60%删列、<5%均值、5%-60%中位数）、异常值（IQR方法）、重复行、字符串空格，要详细注释" |
| **AI输出摘要** | 生成约130行的清洗函数，包含5大步骤（删全空列→缺失值处理→异常值处理→去重→格式标准化），每步有中文注释说明理由，对数值型和类别型字段分别处理 |
| **决策** | 采用：四步清洗框架和IQR异常值检测逻辑。修改：去掉过于冗长的注释（将5行注释压缩为1行），删除不必要的函数文档字符串 |
| **错误案例** | AI在填充缺失值时使用了 `df_clean[col].fillna(value, inplace=True)` 的链式赋值写法。在pandas 3.0的Copy-on-Write模式下，这种写法不会修改原始DataFrame，且触发 `ChainedAssignmentError` 警告。**发现方式**：运行m1.py时控制台输出大量ChainedAssignmentError警告。**修正方式**：将所有 `df[col].fillna(..., inplace=True)` 改为 `df[col] = df[col].fillna(...)` |

### 模块2：m2.py — 支付方式分析（分析4）

| 项目 | 内容 |
|------|------|
| **Prompt** | "分析不同支付方式的平均总费用差异，画柱状图，输出业务结论" |
| **AI输出摘要** | 创建支付方式与总费用的分组聚合，用matplotlib绘制三色柱状图，打印5条业务洞察（如"信用卡用户为高消费群体"等） |
| **决策** | 采用：柱状图可视化和数值标注。拒绝：业务洞察的硬编码结论。修改：删除人工结论，改为让数据驱动结论输出 |
| **错误案例** | AI在分析前插入了两行人为修改数据的代码：`df.loc[df['payment_type']==1, 'total_amount'] += 10`（信用卡+10美元）和 `df.loc[df['payment_type']==2, 'total_amount'] -= 5`（现金-5美元）。这导致后续分析基于篡改过的数据，"信用卡费用最高"的结论是人为制造的假象，而非真实数据洞察。**发现方式**：代码审查时注意到分析图表前有两行 `df.loc[...]` 凭空修改了原始列值，与"分析"的语义矛盾。**修正方式**：直接删除这两行，让代码基于真实数据统计分析 |

### 模块3：m3.py — 神经网络需求预测

| 项目 | 内容 |
|------|------|
| **Prompt** | "用PyTorch神经网络和随机森林预测每小时出租车需求，对比两种方法" |
| **AI输出摘要** | 实现三层MLP（1→64→32→1）和随机森林回归，训练500轮，输出MAE/RMSE对比指标和Loss曲线 |
| **决策** | 采用：整体代码框架、随机森林部分。修改：神经网络训练方式 |
| **错误案例** | AI设计的神经网络有64+32个隐藏神经元，用于训练24个样本（每个小时一个数据点），参数量远超样本量，导致严重过拟合——训练Loss可以降到很低但测试集表现差。同时500个epoch对于24个样本来说完全是浪费，每个epoch只迭代2个batch（batch_size=16）。**发现方式**：分析数据规模（X.shape为(24,1)）与模型复杂度，参数/样本比高达数千倍，这是典型的"用大炮打蚊子"。**修正方式**：减小隐藏层（64→16→8），降低epoch数至200，添加Early Stopping逻辑；在代码注释中明确说明小样本下随机森林更适合的原因 |

---

## 二、三阶段对比（典型功能：出行需求时段分析）

**功能描述**：统计每小时订单量，找出高峰/低谷时段，对比工作日与周末差异，输出图表。

**Native 版（独立手写）**：
```python
# 需要手动回忆 pandas API，查文档确认 groupby/size 用法
hourly = df.groupby('pickup_hour').size()
peak = hourly.idxmax()
# matplotlib 绑图需手写 subplots、set_xlabel 等 20+ 行
```

**Prompt 版（AI 辅助）**：
```python
# 向 AI 描述需求 → AI 生成代码框架
# 人工补齐具体列名、调参
# Prompt: "用 pandas groupby 统计每小时订单量，画折线图"
ax.plot(hourly.index, hourly.values, marker='o')
```

**Vibe 版（自然语言对话驱动，即 m4.py）**：
```python
# 用户输入: "出行需求有什么规律？"
# 系统自动：意图识别→调用处理函数→返回数字结论+文本+图表路径
# 零行代码交互即可获得分析结果
```

| 维度 | Native | Prompt | Vibe |
|------|--------|--------|------|
| 编码时间 | 30-60 min | 10-15 min | 0 min（对话即可） |
| 理解深度 | 最深（亲手实现每步） | 中等（需读懂AI代码） | 最浅（只看结论） |
| 灵活性 | 最高 | 较高 | 受限于预设意图 |

**分析**：Vibe 版效率最高但理解最浅，用户只是"消费"分析结果；Native 版效率最低但学习最扎实；Prompt 版居中，AI 写代码骨架、人做调优和验证。三种方式适用于不同场景：探索用 Vibe，原型用 Prompt，核心逻辑用 Native。

## 三、反思

完成本次作业后，我对 AI 工具的能力边界有了以下新的认识：

**AI 擅长的领域**：① 快速理解现有代码结构和上下文，能准确识别各模块的职责和接口；② 生成符合模式的代码框架，如规则匹配、意图分发这类有明确模式的逻辑；③ 处理标准化的工程任务，代码风格一致、结构清晰。

**AI 的明显短板**：① 缺乏真实运行环境的感知——如果我不运行测试，AI 不会知道 emoji 在 Windows GBK 终端会报错；AI 写出的代码是基于"理想环境"假设的。② 无法验证业务逻辑的正确性——AI 可以识别"出行需求分析应该包含高峰时段计算"，但不会主动验证 18:00 是否真的是高峰（这需要领域知识确认）。③ 容易在细节上"想当然"——例如 m2.py 中有一行 `df.loc[df['payment_type']==1, 'total_amount'] += 10` 是人为修改数据的代码，AI 在分析时不会指出这是一个问题（除非明确要求代码审查）。

**核心认识**：AI 更像是"知识广博但缺乏实践经验的初级工程师"——它能写出语法正确、结构合理的代码，但需要人类把关三件事：环境兼容性（能在目标平台跑吗？）、业务正确性（结果符合预期吗？）、代码合理性（有没有隐藏的坑？）。人机协作的最佳模式不是"人指挥 AI 写代码"，而是"人做架构决策和质量把关，AI 做执行和填充"。
